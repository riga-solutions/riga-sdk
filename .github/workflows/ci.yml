name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Example-id leak gate
        run: node scripts/verify-example-ids.mjs
      - name: Install
        run: npm install
      - name: Build
        run: npm run build
      - name: Type-check
        run: npm run type-check
